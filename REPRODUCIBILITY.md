# Reproducibility

Use the Rust toolchain pinned in `rust-toolchain.toml`. Record the commit SHA, target triple,
CPU model, operating system, compiler version, command, configuration, and random seed for every
published benchmark. Never mix paper-reported numbers with locally measured results.
