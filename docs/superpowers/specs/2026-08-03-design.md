# Design: AegisFlow

The planner/executor boundary is explicit. Requests carry taint labels, policy is deny-by-default, capabilities are scoped and expiring, and audit events form a hash chain. The initial release evaluates requests but deliberately does not expose real operating-system tools.
