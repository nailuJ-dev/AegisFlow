# Governance

The project uses maintainer review. Changes to public APIs, security boundaries, persisted
formats, or scientific assumptions require an Architecture Decision Record. At least one
approving maintainer and passing required checks are needed before merge.
